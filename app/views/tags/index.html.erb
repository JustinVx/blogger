<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%#= tag.name %>
      <%= link_to tag.name, tag_path(tag) %>
      <%#= article.body %>
    </li>
  <% end %>
</ul>

<%= link_to "<< Back to Home Page", articles_path%>
